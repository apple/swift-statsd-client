version: "3"

services:

  runtime-setup:
    image: swift-statsd-client:20.04-5.5
    build:
      args:
        ubuntu_version: "focal"
        swift_version: "5.5"

  test:
    image: swift-statsd-client:20.04-5.5
